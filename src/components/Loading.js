import React from 'react';
import './Loading.css'; 

const Loading = () => {
    return (
        <div id="loading">
            <img src="iconlogo.png" alt="Company Logo" />
        </div>
    );
};

export default Loading;
